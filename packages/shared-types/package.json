{
  "name": "@pulse/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical TaskEvent schema, DTOs, mood enum, and shared constants",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no linter configured yet'"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
